name: AFTT Automation (Weekly)

on:
  schedule:
    - cron: "0 5 * * 0"   # Tous les dimanches à 05:00 UTC
  workflow_dispatch:      # Permet un lancement manuel

jobs:
  run-scripts:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔐 Create Google credentials file
        run: |
          echo "${{ secrets.GOOGLE_CREDENTIALS_JSON }}" > importrencontre.json

      - name: ⚛ Run rencontres
        run: python scripts/script_rencontre.py

      - name: ⚛ Run rencontres vétérans
        run: python scripts/script_rencontre_veterans.py

      - name: 📊 Run classements
        run: python scripts/script_ranking.py

      - name: 📊 Run classements vétérans
        run: python scripts/script_ranking_veterans.py
